[pytest]
testpaths = tests
addopts = -v --tb=short --color=yes -q
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Minimum test coverage threshold (only applies when --cov is used)
# [coverage:report]
# fail_under = 60
